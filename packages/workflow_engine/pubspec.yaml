name: workflow_engine
version: 0.1.0
description: State machine, transitions, and validation for ShipIt Platform workflows
publish_to: none

environment:
  sdk: '>=3.12.0 <4.0.0'

resolution: workspace

dependencies:
  platform_contracts:
    path: ../platform_contracts

dev_dependencies:
  test: ^1.25.2