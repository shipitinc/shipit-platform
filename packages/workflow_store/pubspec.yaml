name: workflow_store
version: 0.1.0
description: Durable persistence and replay for ShipIt Platform work items and human decisions
publish_to: none

environment:
  sdk: '>=3.12.0 <4.0.0'

resolution: workspace

dependencies:
  platform_contracts:
    path: ../platform_contracts
  workflow_engine:
    path: ../workflow_engine

dev_dependencies:
  test: ^1.25.2