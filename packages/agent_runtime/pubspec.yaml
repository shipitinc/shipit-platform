name: agent_runtime
version: 0.1.0
description: Provider-neutral agent runtime interface and adapters
publish_to: none

environment:
  sdk: '>=3.12.0 <4.0.0'

resolution: workspace

dependencies:
  platform_contracts:
    path: ../platform_contracts
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.2