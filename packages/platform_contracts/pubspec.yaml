name: platform_contracts
version: 0.1.0
description: Core domain types, enums, and JSON schemas for ShipIt Platform
publish_to: none

environment:
  sdk: '>=3.12.0 <4.0.0'

resolution: workspace

dependencies:
  json_annotation: ^4.9.0
  equatable: ^2.0.5
  meta: ^1.16.0

dev_dependencies:
  build_runner: ^2.4.11
  json_schema: ^5.2.2
  json_serializable: ^6.8.0
  test: ^1.25.2