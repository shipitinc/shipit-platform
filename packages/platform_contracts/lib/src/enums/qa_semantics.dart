enum EvidenceRowRequirement { required, optional, notApplicable }

enum TestMethod { automated, visual, human, combined }

enum ContractDetermination { expectedToExecute, contingent, skippedByContract }

enum EvidenceDetermination { executed, readyNotExecuted, skipped }
